version: '3'

services:
    hedge:
        build:
          context: ./
          dockerfile: ./docker/Dockerfile    
        container_name: c-hedge
        entrypoint: tail -f /dev/null
        networks:
          - n-hedge        
        volumes:
         - ./:/tmp/hedge

networks:
    n-hedge: